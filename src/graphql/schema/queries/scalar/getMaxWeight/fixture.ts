import type { GetMaxWeightQuery } from '@/graphql/generated/operations-csr'

export const maxWeight: GetMaxWeightQuery = {
  maxWeight: {
    maxWeight: 300,
    createdAt: '2023-06-08T00:00:00.000Z',
  },
}
