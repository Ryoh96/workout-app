query getNotes($since: DateTime, $until: DateTime) {
  notes(since: $since, until: $until) {
    trainings {
      createdAt
      exercise {
        name
        parts {
          name
        }
      }
      rounds {
        ...roundSets
      }
    }
    place {
      name
    }
    createdAt
  }
}
