query getExercise($id: ID!) {
  exercise(id: $id) {
    name
    parts {
      name
    }
    trainings {
      rounds {
        ...roundSets
      }
      createdAt
    }
    movieUrl
    articleUrl
    memos {
      content
      round {
        createdAt
      }
    }
  }
}
