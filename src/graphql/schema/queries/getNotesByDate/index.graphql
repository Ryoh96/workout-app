query getNotesByDate($date: Date!) {
  note(date: $date) {
    trainings {
      exercise {
        name
        parts {
          name
        }
      }
      rounds {
       ...roundSets
      }
   },
   createdAt
  }
}
